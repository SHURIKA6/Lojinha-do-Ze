# Backend Environment Variables Template
# Copy this to .dev.vars for local development or set as secrets in Cloudflare

# Connection string for your PostgreSQL database (e.g., Neon)
DATABASE_URL=postgres://user:password@host/dbname?sslmode=require

# Base URL for the frontend (for CORS and Origin checks)
FRONTEND_URL=https://lojinha-do-ze.vercel.app

# Additional allowed origins for CORS (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000,https://development-branch.vercel.app

# Mercado Pago credentials (get from your MP dashboard)
MERCADO_PAGO_ACCESS_TOKEN=APP_USR-...

# Mercado Pago Webhook Secret (for verifying webhook signatures)
# Get this from: Mercado Pago Dashboard > Your Integration > Webhooks
MERCADO_PAGO_WEBHOOK_SECRET=your_webhook_secret_here

# Optional secret used to generate opaque payment status lookup tokens.
# If omitted, the backend falls back to the webhook secret.
# PAYMENT_LOOKUP_SECRET=your_payment_lookup_secret_here

# Configurações de funcionalidades especiais (opcional)
# Identificadores de e-mail para acesso administrativo ou recursos experimentais
ADMIN_INITIAL_EMAIL=admin@exemplo.com
EASTER_EGG_IDENTIFIER=seu@email.com
ENVIRONMENT=production # production, development, test
JWT_SECRET=your_jwt_secret_here # Se for usar cookies assinados futuramente
