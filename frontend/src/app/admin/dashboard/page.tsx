'use client';

import React from 'react';
import AdminDashboard from '@/features/admin/AdminDashboard';

/**
 * DashboardPage - Rota do painel do administrador
 * A implementação real reside em src/features/admin/AdminDashboard para melhor manutenção.
 */
export default function DashboardPage() {
  return <AdminDashboard />;
}
